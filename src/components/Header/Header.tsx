import NameCard from './NameCard';
import SocialLinks from './SocialLinks';

const Header = () => {
  return (
    <header>
      <NameCard />
      <SocialLinks />
    </header>
  );
};

export default Header; 